import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ironhack.rentapp',
  appName: 'rent-app',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
